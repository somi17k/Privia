<section class="row justify-content-center align-items-center py-3 py-lg-4">
  <div class="col-12 col-xl-11">
    <div class="surface-card overflow-hidden reveal-on-scroll">
      <div class="row g-0">
        <div class="col-lg-5 d-none d-lg-block login-side p-5">
          <span class="section-kicker">Privia Access</span>
          <h3 class="fw-bold mb-3">Secure sign-in for trusted verification workflows</h3>
          <p class="mb-4 text-white-50">
            Access encrypted claims, generate short-lived proofs, and share trust without exposing personal data.
          </p>
          <div class="d-flex flex-column gap-2 small">
            <span><i class="bi bi-lock-fill me-2"></i>AES-encrypted identity fields</span>
            <span><i class="bi bi-patch-check-fill me-2"></i>Admin-approved claims only</span>
            <span><i class="bi bi-clock-history me-2"></i>Time-bound proof lifecycle</span>
          </div>
        </div>

        <div class="col-lg-7 p-4 p-md-5">
          <div class="mb-4">
            <h2 class="fw-bold mb-1">Welcome back</h2>
            <p class="text-muted mb-0">Login to continue your privacy-first verification journey.</p>
          </div>

          <form action="/users/login" method="POST">
            <div class="mb-3">
              <label class="form-label fw-semibold">Email</label>
              <input type="email" name="email" class="form-control form-control-lg" placeholder="you@example.com" required>
            </div>

            <div class="mb-4">
              <label class="form-label fw-semibold">Password</label>
              <input type="password" name="password" class="form-control form-control-lg" placeholder="Enter your password" required>
            </div>

            <button class="btn btn-privia btn-lg w-100" type="submit">
              Login
            </button>
          </form>

          <div class="login-footer d-flex justify-content-between align-items-center flex-wrap gap-2 mt-4">
            <a href="/about" class="text-decoration-none fw-semibold">Learn about Privia</a>
            <p class="mb-0">
              No account?
              <a href="/users/register" class="fw-semibold">Register</a>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .login-side {
    color: #fff;
    background:
      linear-gradient(120deg, rgba(9, 20, 40, 0.7), rgba(16, 33, 63, 0.58)),
      url('https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=1200&q=80') center/cover no-repeat;
  }

  .section-kicker {
    display: inline-block;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    background: rgba(255, 255, 255, 0.14);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 999px;
    padding: 0.25rem 0.7rem;
    margin-bottom: 0.85rem;
  }
</style>
